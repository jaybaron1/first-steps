

# Make "The Roundtable" Stand Out in Hero Subhead

## What Changes
In the hero section subheader, "The Roundtable" will be styled with the Playfair Display italic font and gold color to visually distinguish it as a product name.

## Technical Detail
- In `src/components/sections/HeroSection.tsx`, wrap "The Roundtable" in a styled `<span>` within the subhead `<p>` tag (around line 82)
- Apply `font-display italic` (Playfair Display) and the gold accent color to create contrast against the surrounding DM Sans body text

