// GTM is not active (placeholder ID). Returning null to avoid
// react-helmet-async DOM conflicts that cause removeChild errors.
const GoogleTagManager = () => null;

export default GoogleTagManager;
